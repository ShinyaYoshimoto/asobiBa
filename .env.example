# Database connection URL
# For local development with Docker Compose:
DATABASE_URL="postgresql://asobiba:asobiba@localhost:5432/asobiba_test?schema=public"

# Google Cloud Storage (if needed for photo features)
# GCS_SA_KEY_PATH=path/to/service-account-key.json
# GCS_BUCKET_NAME=your-bucket-name
# GCS_SA_KEY_OBJECT=base64-encoded-key-object
